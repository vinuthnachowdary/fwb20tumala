extends layout

block content
  h1= title
  br
  - var image_names = ["potato.jpg","tomato.jpg","cabbage.jpg","ladiesfinger.jpg","carrot.jpg"]
    - for(let i=0; i<3; i++){
     - let number = Math.floor(Math.random()*5);
       img(src = `/images/${image_names[number]}` alt = "image", height="300", width="300")
    -}